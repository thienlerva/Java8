package Set;

import org.springframework.cglib.core.CollectionUtils;

import java.util.*;
import java.util.stream.Collectors;

public class Subtract {

    public static void main(String[] args) {

//        List<String> dup = Arrays.asList("A", "B", "C", "A");
//
//        Set<String> allItems = new HashSet<>();
//        Set<String> duplicates = dup.stream().filter(n -> !allItems.add(n)).collect(Collectors.toSet());
//        System.out.println(duplicates);
//
//        List<String> lista = Arrays.asList("A", "B", "C", "B");
//        List<String> listb = Arrays.asList("A", "B", "C");
//        Collection<String> subtract = CollectionUtils.substract(lista, listb);

    }
}
